/// A modular for the `build` errors
pub mod build;

/// A modular for the `config` errors
pub mod config;

/// A modular for the `lexer` errors
pub mod lexer;

/// A modular for the `linter` errors
pub mod linter;

/// A modular for the `package` errors
pub mod package;
/// A modular for the `parser` errors
pub mod parser;

/// A modular for the `semantic` errors
pub mod semantic;

// Re-export
// pub use lexer::LexerError;
// pub use parser::ParserError;
// pub use semantic::SemanticError;
// pub use package::PackageError;
// pub use build::BuildError;
// pub use config::ConfigError;
// pub use lint::LintWarning;
